import React from 'react'
// import { useUser } from '@/auth/UserContext';

export default function Dashboard() {
  // const { user, loading, logout } = useUser();

  return (
    <div className="text-black text-9xl">Dashboard</div>
  )
}

